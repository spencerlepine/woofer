name: branch_check

on:
  push:
    branches:
      - '*'
      - '!main'

jobs:

  test-client:
    uses: ./.github/workflows/test.yml
    secrets:
      SUB_FOLDER: "./server"

  test-server:
    needs: [test-client]
    uses: ./.github/workflows/test.yml
    secrets:
      SUB_FOLDER: "./server"

  build:
    needs: [test-server]
    uses: ./.github/workflows/build.yml