name: deploy

on:
  push:
    branches:
      - main

jobs:

  test:
    uses: ./.github/workflows/test.yml

  deploy:
    test:
      uses: ./.github/workflows/deploy.yml
      with:
        DOCKER_USERNANE: spencerlepine
        DOCKER_REPO: woofer-dev