name: deploy

on:
  push:
    branches:
      - main

jobs:

  test:
    uses: ./.github/workflows/test.yml

  deploy:
    uses: ./.github/workflows/deploy.yml
    env:
      DOCKER_USERNAME: spencerlepine
      DOCKER_REPO: woofer