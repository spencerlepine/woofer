name: deploy

on:
  push:
    branches:
      - main

jobs:

  test:
    uses: ./.github/workflows/test.yml

  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      docker_username: spencerlepine
      docker_repo: woofer