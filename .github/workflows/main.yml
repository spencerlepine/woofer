name: deploy

on:
  push:
    branches:
      - main

jobs:

  test:
    uses: ./.github/workflows/test.yml

  build:
    needs: [test]
    uses: ./.github/workflows/build.yml

  deploy:
    needs: [build]
    uses: ./.github/workflows/deploy.yml